#!/bin/sh

TARGET_NAME=`basename ${WAR_FILE_URL}`
DEPLOY_DIR=$JBOSS_HOME/standalone/deployments
echo "Installing ${WAR_FILE_URL} as a deployment"
curl -o ${DEPLOY_DIR}/${TARGET_NAME} -O ${WAR_FILE_URL}

exit 0
